zad 1
1) SELECT AVG(pensja) FROM EMPLOYEE;
2) SELECT JOB_POSITION_ID, AVG(pensja) FROm EMPLOYEE GROUP BY JOB_POSITION_ID;
3) SELECT DEPARTMENT_ID, AVG(YEAR(CURRDATE() - YEAR(e.BIRTH_DATE) as SREDNI_WIEK)) FROM EMPLOYEE GROUP BY DEPARTMENT_ID;

zad 2
1) SELECT DEPARTMENT_ID, SUM(pensja) FROM EMPLOYEE GROUP BY DEPARTMENT_ID ORDER BY SUM(pensja) DESC;
2) SELECT JOB_POSITION_ID, AVG(pensja) FROM EMPLOYEE GROUP BY JOB_POSITION_ID HAVING COUNT(*) >= 3 AND AVG(pensja) > 3000;
3) SELECT DEPARTMENT_ID, AVG(pensja) FROM EMPLOYEE GROUP BY DEPARTMENT_ID HAVING COUNT (*) > 3 ORDER BY pensja ASC;

zad 3
1) CREATE TABLE DEPARTMENT AS SELECT * FROM DEPARTMENT;
2) CREATE TABLE JOB_POSITION AS SELECT * FROM JOB_POSITION;
3) SELECT d.DEPARTMENT_NAME, COUNT(*) FROM DEPARTMENT d JOIN EMPLOYEE e ON e.DEPARTMENT_ID = d.DEPARTMENT_ID GROUP BY d.DEPARTMENT_NAME;

zad 4
1) SELECT e.LAST_NAME, e.JOB_POSITION_ID FROM EMPLOYEE e WHERE EXTRACT(YEAR FROM E.BIRTH_DATE) BETWEEN 1990 AND 1999;
2) SELECT e.FIRST_NAME, e.LAST_NAME FROM EMPLOYEE e WHERE e.JOB_POSITION_ID = (SELECT JOB_POSITION_ID FROM JOB_POSITION WHERE JOB_TITLE = 'Magazynier') ORDER BY e.BIRTH_DATE DESC LIMIT 5;
3) SELECT c1.CLIENT_NAME || ' - ' || c2.CLIENT_NAME FROM CLIENT c1 JOIN CLIENT c2 ON ABS(c1.CLIENT_ID - c2.CLIENT_ID) = 5;

zad 5
1) SELECT JOB_POSITION_ID, AVG(pensja) FROM EMPLOYEE WHERE DEPARTMENT_IDNOIT IN  ( SELECT DEPARTMENT_ID FROM DEPARTMENT WHERE DEPARTMENT_NAME
2) SELECT JOB_POSITION_ID, MIN(BIRTH_DATE) AS NAJSTARZSA_OSOBA, MAX(BIRTH_DATE) AS NAJMLODSZA_OSOBA, DEPARTMENT_ID FROM EMPLOYEE GROUP BY JOB_POSITION_ID, DEPARTMENT_ID;
