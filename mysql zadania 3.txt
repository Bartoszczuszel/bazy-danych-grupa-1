Zad 1
1a) CREATE TABLE CLIENT AS SELECT * FROM company_2025.CLIENT;
1b) CREATE TABLE ORDER AS SELECT * FROM company_2025.ORDER;
1c) CREATE TABLE EMPLOYEE AS SELECT * FROM company_2025.EMPLOYEE;
2) SELECT * FROM ORDER;
3) SELECT * FROM ORDER WHERE ORDER_STATUS =5;
4) SELECT ORDER_ID,, ORDER_STATUS FROM ORDER WHERE CLIENT_ID IN (1,3,5);
 
Zad 2 
1) SELECT * FROM EMPLOYEE WHERE FIRST_NAME = 'Agnieszka' AND pensja = 3900;
2) SELECT EMPLOYEE_NAME, FIRST_NAME, LAST_NAME pensja FROM EMPLOYEE WHERE pensja BETWEEN 5000 AND 7000;
3) SELECT * FROM CLIENT WHERE SHORT_NAME ILIKE '%eco' AND IS_COMPANY =1;
 
Zad 3 
1) SELECT * FROM ORDER WHERE EXTRACT(MONTH FROM ORDER_DATE) IN (6,7); 
2) SELECT CLIENT_ID, SHORT_NAME, TAX_IDENTIFIER FROM CLIENT WHERE TAX_IDENTIFIER is not null  and TAX_IDENTIFIER <> ''
ORDER BY SHORT_NAME ASC;
3) SELECT EMPLOYEE_NAME, FIRST_NAME, LAST_NAME, DATE_OF_EMPLOYMENt, MONTH(DATE_OF_EMPLOYMENT) AS MONTH_OF_EMPLOYMENT FROM EMPLOYEE ORDERY BY DATE_OF_EMPLOYMENt ASC LIMIT 5;
 
 
Zad 4
1) SELECT DISTINCT FIRST_NAME FROM EMPLOYEE ORDERY BY FIRST_NAME;
2) SELECT CONCAT(FIRST_NAME, ' - ', LAST_NAM) as full_name FROM EMPLOYEE WHERE LAST_NAME LIKE 'Ko%';
3) SELECT LAST_NAME,pensja, DATE_OF_EMPLOYMENT FROM EMPLOYEE WHERE DATE_OF_EMPLOYMENT >= DATE '2010-01-01' AND  DATE_OF_EMPLOYMENT < DATE '2015-01-01' ORDER BY DATE_OF_EMPLOYMENT;
 
Zad 5
1)SELECT EMPLOYEE_NAME, pensja AS gross_salary, ROUND(pensja *(1.0 - 0.25), 2) AS net_salary, ROUND(pensja * 0.25, 2)
AS tax_value FROM EMPLOYEE;
2) SELECT * FROM CLIENT WHERE TAX_IDENTIFIER IS NULL OR TAX_IDENTIFIER = '';
3) SELECT SHORT_NAME FROM CLIENT WHERE SHORT_NAME LIKE 'K%' OR SHORT_NAME LIKE '%ski' ORDER BY SHORT_NAME ASC;